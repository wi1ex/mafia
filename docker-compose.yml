networks:
  edge:
    driver: bridge
  internal:
    driver: bridge
    internal: true

volumes:
  pg_data:
  redis_data:
  minio_data:

services:
  db:
    image: postgres:17-alpine
    container_name: db
    env_file: .env
    volumes: ["pg_data:/var/lib/postgresql/data"]
    networks: [internal]
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB -h localhost -p $$POSTGRES_PORT || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7.2-alpine
    container_name: redis
    env_file: .env
    command:
      - redis-server
      - --requirepass
      - "${REDIS_PASSWORD}"
      - --loglevel
      - "warning"
      - --appendonly
      - "yes"
      - --appendfsync
      - "everysec"
    volumes: ["redis_data:/data"]
    networks: [internal]
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "$REDIS_PASSWORD", "PING"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: minio/minio:RELEASE.2025-07-18T21-56-31Z
    container_name: minio
    env_file: .env
    command: server /data
    volumes: ["minio_data:/data"]
    networks: [internal]
    restart: always

  backend:
    build: ./backend
    container_name: backend
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    networks: [internal]
    restart: always
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:8000/api/v1/health/ping"]
      interval: 10s
      timeout: 3s
      retries: 10

  livekit:
    image: livekit/livekit-server:v1.9.0
    container_name: livekit
    network_mode: "host"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    environment:
      LIVEKIT_CONFIG: |
        port: ${LIVEKIT_HTTP_PORT}
        log_level: info
        region: eu-nl
        rtc:
          port_range_start: ${LIVEKIT_PORT_RANGE_START}
          port_range_end: ${LIVEKIT_PORT_RANGE_END}
          tcp_port: ${LIVEKIT_TCP_PORT}
          use_external_ip: true
        turn:
          enabled: true
          domain: ${DOMAIN}
          udp_port: ${LIVEKIT_UDP_PORT}
          tls_port: ${LIVEKIT_TLS_PORT}
          cert_file: ${SSL_CERT_PATH}
          key_file: ${SSL_KEY_PATH}
      LIVEKIT_KEYS: |
        ${LIVEKIT_API_KEY}: ${LIVEKIT_API_SECRET}
    restart: always

  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
      args:
        VITE_API_BASE: "/api"
        VITE_TG_BOT_NAME: "${TG_BOT_NAME}"
    container_name: nginx
    env_file: .env
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    ports:
      - "80:80/tcp"
      - "443:443/tcp"
    depends_on:
      backend:
        condition: service_healthy
      livekit:
        condition: service_started
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks: [edge, internal]
    restart: always
